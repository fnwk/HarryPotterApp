import { View } from "react-native";
import ThemedText from "@/components/common/ThemedText";

const GamesScreen = () => {
  return (
    <View>
      <ThemedText>Testowy widok gier</ThemedText>
    </View>
  );
};

export default GamesScreen;
